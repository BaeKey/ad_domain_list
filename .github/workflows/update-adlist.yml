name: Update AdBlock List

on:
  schedule:
    - cron: '0 20 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          pip install requests

      - name: Process Domain Lists
        run: |
          python - <<EOF
          import requests
          import os

          # ================= 配置区域 =================
          AD_LIST_URL = "https://raw.githubusercontent.com/privacy-protection-tools/anti-AD/master/anti-ad-domains.txt"

          REMOTE_WHITELISTS = [
              "https://raw.githubusercontent.com/privacy-protection-tools/anti-AD/refs/heads/master/discretion/dns.txt",
              "https://raw.githubusercontent.com/privacy-protection-tools/anti-AD/refs/heads/master/discretion/anv.txt"
          ]

          HAGEZI_LIST_URL = "https://raw.githubusercontent.com/hagezi/dns-blocklists/refs/heads/main/domains/multi.txt"

          LOCAL_WHITELIST_FILE = "whitelist.txt"
          OUTPUT_FILE = "clean-list.txt"
          # ===========================================

          def download_text(url):
              try:
                  print(f"Downloading: {url}")
                  resp = requests.get(url, timeout=30)
                  resp.raise_for_status()
                  return resp.text
              except Exception as e:
                  print(f"Error downloading {url}: {e}")
                  return ""

          def parse_lines(text):
              lines = set()
              for line in text.splitlines():
                  line = line.strip()
                  if not line or line.startswith("#"):
                      continue
                  lines.add(line)
              return lines

          def main():
              # --- 1. anti-AD 主列表 ---
              ad_text = download_text(AD_LIST_URL)
              ad_domains = parse_lines(ad_text)
              original_count = len(ad_domains)
              print(f"原始广告域名数量: {original_count}")

              # --- 2. 远程白名单（精确匹配） ---
              remote_wl_domains = set()
              for url in REMOTE_WHITELISTS:
                  remote_wl_domains.update(parse_lines(download_text(url)))

              before_remote = len(ad_domains)
              ad_domains -= remote_wl_domains
              removed_by_remote = before_remote - len(ad_domains)
              print(f"远程白名单移除数量: {removed_by_remote}")

              # --- 3. 本地白名单（子域名匹配） ---
              user_rules = set()
              if os.path.exists(LOCAL_WHITELIST_FILE):
                  with open(LOCAL_WHITELIST_FILE, "r", encoding="utf-8") as f:
                      user_rules = parse_lines(f.read())

              removed_by_user = 0
              if user_rules:
                  filtered = []
                  for domain in ad_domains:
                      if any(domain == r or domain.endswith("." + r) for r in user_rules):
                          removed_by_user += 1
                      else:
                          filtered.append(domain)
                  ad_domains = set(filtered)

              print(f"用户白名单移除数量: {removed_by_user}")

              # --- 4. 与 hagezi light 取交集 ---
              hagezi_domains = parse_lines(download_text(HAGEZI_LIST_URL))
              before_intersect = len(ad_domains)
              ad_domains &= hagezi_domains
              removed_by_intersect = before_intersect - len(ad_domains)

              print(f"与 hagezi light 取交集移除数量: {removed_by_intersect}")

              # --- 5. GitHub Summary ---
              final_count = len(ad_domains)
              summary = os.environ.get("GITHUB_STEP_SUMMARY")
              if summary:
                  with open(summary, "a", encoding="utf-8") as f:
                      f.write("### 🛡️ 广告过滤统计报告\n")
                      f.write("| 项目 | 数量 |\n")
                      f.write("| :--- | :--- |\n")
                      f.write(f"| 📥 anti-AD 原始 | **{original_count}** |\n")
                      f.write(f"| 🌍 远程白名单 | -{removed_by_remote} |\n")
                      f.write(f"| 👤 本地白名单 | -{removed_by_user} |\n")
                      f.write(f"| 🔗 hagezi 交集 | -{removed_by_intersect} |\n")
                      f.write(f"| ✅ **最终数量** | **{final_count}** |\n")

              # --- 6. 写入最终文件 ---
              with open(OUTPUT_FILE, "w", encoding="utf-8") as f:
                  for domain in sorted(ad_domains):
                      f.write(domain + "\n")

              print(f"完成，输出至 {OUTPUT_FILE}")

          if __name__ == "__main__":
              main()
          EOF

      - name: Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add clean-list.txt
          git diff --quiet && git diff --staged --quiet || (
            git commit -m "Update clean adblock list" &&
            git push
          )
